/// <summary>
/// Represents an interface for data readers.
/// </summary>
public interface IDataReader
{
    /// <summary>
    /// Reads data from a source and returns it as a string.
    /// </summary>
    /// <returns>The read data as a string.</returns>
    string ReadData();
}